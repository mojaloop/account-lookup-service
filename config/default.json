{
  "ADMIN_PORT": 4001,
  "API_PORT": 4002,
  "DATABASE_URI": "mysql://account_lookup:password@localhost:3307/account_lookup",
  "ENDPOINT_CACHE_CONFIG": {
    "expiresIn": 180000,
    "generateTimeout": 30000
  },
  "SWITCH_ENDPOINT": "http://localhost:3001",
  "INSTRUMENTATION": {
    "METRICS": {
      "DISABLED": false,
      "labels": {
        "fspId": "*"
      },
      "config": {
        "timeout": 5000,
        "prefix": "moja_",
        "defaultLabels": {
          "serviceName": "account-lookup-service"
        }
      }
    }
  }
}
