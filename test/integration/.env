# REDIS CLUSTER
# macOS or Windows only
# - Update the `REDIS_CLUSTER_ANNOUNCE_IP` to the host machine's external IP.
#   To retrieve the host machine's external IP in macOS, run `echo "$(route get ifconfig.me | grep interface | sed -e 's/.*: //' | xargs ipconfig getifaddr)"` in the terminal.
#   To retrieve the host machine's external IP in Windows, run `ipconfig` in the command prompt and look for the `IPv4 Address` under the `Wireless LAN adapter Wi-Fi` section.
#   For Linux, use the Docker default bridge network IP ` echo $(ip addr show docker0 | grep 'inet ' | awk '{print $2}' | cut -d/ -f1)` which is most likely `172.17.0.1`
REDIS_CLUSTER_ANNOUNCE_IP=172.17.0.1 # Docker default bridge network IP
#PROXY_HOST=localhost
PROXY_HOST=proxy
PROXY_PORT=4300
PROXY_NAME=proxyXY

#CL_HOST=localhost
CL_HOST=central-ledger
CL_PORT=3001
